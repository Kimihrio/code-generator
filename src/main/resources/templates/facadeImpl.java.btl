package ${facade.FacadeImpl};

import ${facade.Facade}.${facade.FacadeName};
import ${bll.BLL}.${bll.BLLName};
import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;

/**
 * ${table.comment} 门面实现类
 * @author ${author}
 * @since ${date}
 */
@Service
public class ${facade.FacadeImplName} implements ${facade.FacadeName} {

    @Autowired
    private ${bll.BLLName} bll;

}
